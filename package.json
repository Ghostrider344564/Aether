{
  "name": "aether",
  "private": true,
  "packageManager": "npm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "isolated-vm": "^6.1.2"
  }
}
